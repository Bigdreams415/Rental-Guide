name: rental_guide
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  iconsax: ^0.0.8
  provider: ^6.1.1
  http: ^1.1.0
  flutter_secure_storage: ^9.2.4
  image_picker: ^1.0.4  
  video_player: ^2.8.1   
  youtube_player_flutter: ^8.1.1
  path: ^1.8.3
  flutter_svg: ^2.0.9
  nigeria_lg_state_city: ^0.0.3
  url_launcher: ^6.3.0
  # For actual Google Sign-In later:
  # google_sign_in: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/google.svg